// Chat server ports
export const PORTS = ['3001', '3002', '3003', '3004'];

// No Recipient
export const NO_RECIPIENT = 'Choose someone to message';

// Initial status prompt
export const INITIAL_PROMPT = 'Select a user and port.';

// Recipient disconnected
export const RECIPIENT_LOST = 'has disconnected.';

// Recipient reconnected
export const RECIPIENT_FOUND = 'has reconnected.';